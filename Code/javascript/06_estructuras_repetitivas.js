var persona = {// estudiante es un Objeto
    nombre: 'Jose',
    apellido:'Valverde',
    // afiliacion:'UPS',
    correo_electronico: 'fvalverde@est.ups.edu.ec'
    peso: 120,

} 
const INCREMENTAR_PESO =3
const DECREMENTAR_PESO =

const aumentarPeso =  (objeto) => objeto.peso += INCREMENTAR_PESO
const disminuyePeso =  (objeto) => objeto.peso -= DECREMENTAR_PESO

const comeMucho = () => Math.random()<0.3
const realizaDeporte = () => Math.random()<0.6
 
const META  = estudiante.peso-10 // constante y el nombre e sen mayuscula, y no se permite modificar valor
while (persona.peso > meta){
    if (comeMucho ()){
        aumentaPeso(persona)
    }
    if (reaizaDeporte(){
        disminuyePeso(persona)
    })
}
}

console.log(
)